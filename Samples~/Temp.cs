using System;

namespace TGL.EventBus.Sample // tgl-event-bus.samples and tglGames.tgl-event-bus.samples
{
    // Order of data Flow
    // --- Events base ---
    // --- Concrete Channels (One-Line Definitions) ---
    // --- Concrete Events (Must be 'readonly record struct' or 'struct' to satisfy `where TEvent : struct` condition) ---
}
